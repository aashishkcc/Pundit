const Button = ({styles}) => {
  return (
    <button type="button" className={`py-4 px-[40px] bg-blue-gradient font-poppins font-semibold sm:text-[30px] text-[20px] text-white outline-none rounded-2xl ${styles}`}>Contact Us Below</button>
  )
}

export default Button